# zh
* 依赖管理: 在找不到某个包时显示"安装"按钮而不是"更新"
* 依赖管理: 修改检测破坏性更新的逻辑，使其更符合semver规范
# en
* Dependencies Mgmt: display "install" button when package is missing instead of misleading "update".
* Dependencies Mgmt: detecting break changes for updating now works properly according to semver spec